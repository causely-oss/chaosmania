apiVersion: apps/v1
kind: Deployment
metadata:
  name: betsettlement
  labels:
    {{include "common.labels" "betsettlement" | nindent 4}}
spec:
  replicas: 2
  selector:
    matchLabels:
      {{include "common.labels" "betsettlement" | nindent 6}}
  template:
    metadata:
      labels:
        {{include "common.labels" "betsettlement" | nindent 8}}
    spec:
      containers: 
      {{include "chaosmania.container" . | nindent 6}}
          - name: ENABLED_BACKGROUND_SERVICES
            value: "{{ .Values.betsettlement.background_services | join "," }}"
      volumes:
      {{include "common.volumes" . | nindent 6}}
---
{{include "chaosmania.service" "betsettlement"}}
---
{{ if .Values.hpa.enabled }}
{{ include "chaosmania.hpa" "betsettlement" }}
{{ end }}